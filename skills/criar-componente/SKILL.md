---
name: criar-componente
description: Cria componentes, hooks e testes TypeScript reutilizaveis com registro no dashboard.
user-invocable: false
---

# Criar componente

Leia `devLevel` em `dashboard-config.json`. Siga `./skills/react-dev/references/dashboard-projetos.md` para o perfil.

Crie componentes funcionais tipados. Separe hook, apresentacao e dados. Registre via `POST /api/components`.